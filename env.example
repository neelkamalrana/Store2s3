# Store2S3 Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# AWS S3 Configuration (REQUIRED)
# =============================================================================
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=us-east-1
S3_BUCKET_NAME=your_s3_bucket_name_here

# =============================================================================
# AWS Cognito Configuration (REQUIRED)
# =============================================================================
AWS_COGNITO_USER_POOL_ID=your_user_pool_id_here
AWS_COGNITO_CLIENT_ID=your_app_client_id_here
AWS_COGNITO_REGION=us-east-1

# =============================================================================
# Server Configuration
# =============================================================================
PORT=5001
NODE_ENV=development

# =============================================================================
# AWS Cognito Setup Instructions
# =============================================================================
# 1. Go to AWS Console → Cognito service
# 2. Create User Pool:
#    - Sign-in options: Email
#    - Password policy: Default or custom
#    - MFA: Optional
#    - User account recovery: Enable email recovery
# 3. Configure App Integration:
#    - App client name: store2s3-web-client
#    - Client secret: Uncheck (for web apps)
#    - Authentication flows: Enable "ALLOW_USER_SRP_AUTH"
# 4. Copy User Pool ID and App Client ID to this file

# =============================================================================
# Security Notes
# =============================================================================
# - Never commit .env files to version control
# - Use different credentials for development and production
# - Rotate your AWS keys regularly
# - Use IAM roles when possible instead of access keys
